// Root project is a container for subprojects only.
// The 1.20.4 fabric client previously living here references a large
// framework (GlacierClient, EventBus, HUDMod, settings system, etc.)
// that does not exist in this repo. Until that framework is in place,
// the root project produces no artifacts and only aggregates subprojects.

group = 'net.glacierclient'
version = '1.0.0'
